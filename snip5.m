disp("To display the eigenvalues of the matrix");
eigenval = eig(a)
disp(" The eigenvalues are the diagonal elements for a Diagonal matrix ");

% This snippet computes the eigenvalues of matrix a using the eig function and prints them.
% It then states that for a diagonal matrix, the eigenvalues are equal to its diagonal elements.
